#!/bin/bash

# Enable SPI interface
if grep -q -E "^#?dtparam=spi=on" /boot/config.txt; then
	sed -i 's/^#\?dtparam=spi=on.*/dtparam=spi=on/' /boot/config.txt
else
	echo "dtparam=spi=on" >> /boot/config.txt
fi
